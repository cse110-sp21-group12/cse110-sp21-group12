/**
 * Mock function to GET a password hash
 */
export function mockGETPasswordHash(){
    return 1083;
}

/**
 * Mock function to add a new user record to the database
 * @param {*} userObject A user object containing Username, Password Hash, and Theme ID
 */
export function mockPUTUser(userObject){
    //
}