/**
 * prints hello world!
 */
function helloWorld() {
    console.log('hello world');
}

/**
 * takes in 2 numbers and adds them
 * @param {Number} a
 * @param {Number} b
 * @returns {Number} The sum of a and b
 */
function adder(a, b) {
    return a + b;
}

helloWorld();
adder(1, 1);
